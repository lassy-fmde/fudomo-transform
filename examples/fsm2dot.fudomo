# Dot-file content
FSM.dot:
  header, states, transitions, footer

# List of states
FSM.states:
  cont -> State.name

# List of transitions
FSM.transitions:
  cont -> Transition.rep

# Representation of a transition
Transition.rep:
  source -> State.name, target -> State.name

FSM.header:
FSM.footer:
